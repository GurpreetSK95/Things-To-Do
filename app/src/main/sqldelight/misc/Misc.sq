CREATE TABLE misc (
  id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  notes TEXT NOT NULL,
  timestamp INTEGER NOT NULL
);

selectAll:
SELECT * FROM misc ORDER BY timestamp ASC;

insertOne:
INSERT INTO misc (name, notes, timestamp) VALUES (?, ?, ?);
